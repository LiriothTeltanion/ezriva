# Start Here

Ezriva is the locked project name. Product planning already exists.

This file is a navigation index only. It does not replace the product contract,
checklist, current Git state, or dated verification evidence.

1. Confirm that the selected Git root is this Ezriva repository on `main`.
2. Read [`CODEX-HANDOFF.md`](CODEX-HANDOFF.md) completely.
3. Follow its mandatory reading order, beginning with `AGENTS.md`, before
   planning or implementation.
4. Treat the five canonical files under `docs/hackathon-build/` as the product
   contract; treat journals and dated evaluations as evidence, not authority.
5. Reconcile every status claim against the current worktree and fresh checks.
6. Keep implementation work inside this repository.

Never open, import, copy, or summarize `project_sources/`. Never read or display
real environment values, credentials, tokens, private documents, provider
request/account IDs, prompts, raw provider traces, or raw provider errors. Safe,
redacted activity events with tool names and states are allowed when the product
contract requires them.

Do not commit, push, publish, deploy, create AWS resources, invoke a metered
model, submit a credit request, or mutate an external account without Kevin's
explicit approval for that action.
